{
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "postbuild": "cp package*.json ../public && cd ../public && npm ci --omit=dev && rm ../public/package*.json"
  },
  "dependencies": {
    "chart.js": "^4.4.2",
    "lit": "^3.1.2"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "lit-analyzer": "^2.0.3",
    "prettier": "^3.2.5",
    "typescript": "~5.4.5"
  },
  "customElements": "custom-elements.json"
}
